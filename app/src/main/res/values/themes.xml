<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RemoteControlCar" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_300</item>
        <item name="colorPrimaryVariant">@color/blue_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_300</item>
        <item name="colorSecondaryVariant">@color/blue_100</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.RemoteControlCar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.RemoteControlCar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RemoteControlCar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.RemoteControlCar.FAB" parent="Theme.MaterialComponents">
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:scaleType">centerInside</item>
        <item name="hoveredFocusedTranslationZ">0dp</item>
        <item name="rippleColor">@color/blue_100</item>
        <item name="pressedTranslationZ">2dp</item>
        <item name="tint">@color/blue_300</item>
        <item name="fabCustomSize">80dp</item>
        <item name="maxImageSize">48dp</item>
        <item name="borderWidth">0dp</item>
        <item name="elevation">0dp</item>
    </style>
</resources>